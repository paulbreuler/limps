# Review Branch (General)

Review changes on the current branch compared to `main`.

## Invocation

```
/review-branch [scope]
```

## Instructions for Claude

1. Invoke the skill: `/branch-code-review $ARGUMENTS`
2. If no arguments are provided, review `base...HEAD` (auto-detected base).
